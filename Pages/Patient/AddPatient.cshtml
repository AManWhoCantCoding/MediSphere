@page
@model MediSphere.Pages.Patient.AddPatientModel
@{
    ViewData["Title"] = Localizer["AddPatient_Title"].Value;
    Layout = "~/Pages/Shared/_DashboardLayout.cshtml";
}

<div class="ms-page-header">
    <h2>@Localizer["AddPatient_Title"]</h2>
    <p>@Localizer["AddPatient_Subtitle"]</p>
</div>

<div class="ms-card ms-form-card">
    <form id="add-patient-form">
        <div id="form-errors" class="text-danger mb-3"></div>
        <div class="form-floating mb-3">
            <input id="firstName" class="form-control" required />
            <label>@Localizer["FirstName"]</label>
        </div>
        <div class="form-floating mb-3">
            <input id="lastName" class="form-control" required />
            <label>@Localizer["LastName"]</label>
        </div>
        <div class="form-floating mb-3">
            <input id="emailAddress" class="form-control" type="email" />
            <label>@Localizer["EmailAddress"]</label>
        </div>
        <div class="form-floating mb-3">
            <input id="contactNumber" class="form-control" />
            <label>@Localizer["PhoneNumber"]</label>
        </div>
        <div class="form-floating mb-3">
            <select id="gender" class="form-control">
                <partial name="_GenderOptionsPartial" />
            </select>
            <label>@Localizer["SelectGender"]</label>
        </div>
        <div class="form-floating mb-3">
            <input id="dateOfBirth" type="date" class="form-control" />
            <label>@Localizer["DateOfBirth"]</label>
        </div>
        <div class="form-check mb-4">
            <input id="isPrivatePatient" class="form-check-input" type="checkbox" />
            <label class="form-check-label">@Localizer["IsPrivatePatient"]</label>
        </div>
        <button type="submit" class="btn btn-primary w-100">@Localizer["AddPatient_Button"]</button>
    </form>
</div>

@section Scripts {
    <script src="~/js/patients.js" asp-append-version="true"></script>
}
